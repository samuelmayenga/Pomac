@model IEnumerable<POMACSystem.Models.Relief>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReliefName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReliefName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReliefID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReliefID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReliefID })
        </td>
    </tr>
}

</table>
